<?xml version="1.0" encoding="UTF-8"?>
<!--
  PLACEHOLDER — configure in Salesforce Setup after deploying.

  Steps:
  1. In Data Cloud, create an Ingestion API Connector named "SubmissionDocuments"
     mapped to entity ADL_AgentforcePOC_D__dlm.
  2. Setup > Auth. Providers: create an OAuth provider for your Data Cloud
     Connected App (Client ID + Secret).
  3. Setup > Named Credentials > External Credentials: create "DataCloud_Ingestion_API_EC"
     with Protocol = OAuth 2.0 and the Auth Provider above.
  4. Setup > Named Credentials > DataCloud_Ingestion_API > Edit:
     set URL = https://<your-dc-tenant>.c360a.salesforce.com,
     link to the External Credential, then click Authenticate.
-->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <label>Data Cloud Ingestion API</label>
</NamedCredential>
